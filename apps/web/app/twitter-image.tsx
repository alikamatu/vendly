// Twitter image: re-uses the OG image since the dimensions and styling
// translate cleanly. Next will serve this at /twitter-image for the root.
export { default, runtime, alt, size, contentType } from "./opengraph-image";
